version: v1.0
name: Initial Pipeline
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu2004
blocks:
  - name: Deploy to Manifests Database
    task:
      jobs:
        - name: Depoy
          commands:
            - echo $SEMAPHORE_GIT_SHA > $SEMAPHORE_PROJECT_NAME.sha.manifest
            - ls
            - cat $SEMAPHORE_PROJECT_NAME.sha.manifest
